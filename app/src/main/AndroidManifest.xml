<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.psoik.andrey.getsmartkid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.MainActivity" />
        <activity
            android:name=".Activitys.AddNewTaskActivity"
            android:label="Новая задача" />
        <activity
            android:name=".Activitys.CheckChildrenActivity"
            android:label="Мои дети" />
        <activity
            android:name=".Activitys.AddNewProduct"
            android:label="Новая награда" />
        <activity
            android:name=".Activitys.AddNewChildren"
            android:label="Добавить ребенка" />
        <activity android:name=".Activitys.DetailTask"
            android:label="Задание"/>
        <activity android:name=".Activitys.ContributionActivity"
            android:label="Вклады"/>
        <activity android:name=".Activitys.LoanActivity"
            android:label="Кредиты"/>
    </application>

</manifest>